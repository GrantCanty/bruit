package bruit

/*type WebSocketClient interface {
	// GENERAL METHODS
	InitWebSockets(g *Settings)
	DeferChanClose(g *Settings)

	// PUBLIC SOCKET METHODS
	SubscribeToOHLC(g *Settings, pairs []string, interval int)
	PubDecoder(g *Settings)
	PubListen(g *Settings, ohlcMap *types.OHLCVals, tradesWriter *api.WriteAPIImpl) // needs to take an interface instead of OHLCVals
	//SubscribeToTrades()

	// ORDER BOOK SOCKET METHODS
	SubscribeToOrderBook(g *Settings, pairs []string, depth int)
	//BookDecode(g *ConcurrencySettings)
	BookListen(g *Settings)

	// PRIVATE SOCKET METHODS

	//PrivListen()
}*/
